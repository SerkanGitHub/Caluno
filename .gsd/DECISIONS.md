# Architectural Decisions

*Append-only register of architectural and pattern decisions. Read during planning/research. Append during execution when meaningful decisions are made.*

## Decisions

*(No decisions recorded yet)*

---

**Format for new entries:**

```
## YYYY-MM-DD: Decision Title

**Context:** Brief description of the problem/choice
**Decision:** What was decided
**Rationale:** Why this choice was made
**Consequences:** Expected impacts, tradeoffs
```

---

## Decisions Table

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M001 | library | Authoritative implementation stack | Use the existing SvelteKit web app and SvelteKit + Capacitor mobile shell as the implementation stack; ignore the stale React Native / Flutter references in uploaded notes. | The repository already defines the real implementation path, and planning against it avoids fake architecture and unnecessary migration work. | Yes — only if the product direction later justifies an explicit stack migration. | human |
| D002 | M001 | architecture | Backend access pattern for the scheduling substrate | Use Supabase for auth, database, realtime, and RLS, with a thin SvelteKit server layer for privileged or composed operations only; do not add GraphQL in M001. | This keeps the backend simple, fits the chosen stack, preserves a strong auth/RLS story, and avoids inventing an extra API layer before the substrate exists. | Yes — revisit only if future milestones expose composition needs Supabase plus thin server routes cannot handle cleanly. | human |
| D003 | M001 | architecture | Sync authority model | Clients are local-first, but the server is canonical after reconnect. | Caluno needs real offline continuity, but shared schedules become untrustworthy if reconciliation is left ambiguous across devices or collaborators. | Yes — revisit only if later evidence shows a different conflict model is necessary. | human |
| D004 | M001 | architecture | Primary proof surface for the first milestone | M001 is web-first for proof and verification, because the browser is the available realistic test surface. | The user needs to simulate and validate the substrate in the browser now, and does not have reliable phone hardware available for device-first proof. | Yes — later milestones should rebalance toward mobile parity once the substrate exists. | human |
| D005 | M001 | library | Web local persistence direction | Use browser SQLite/WASM as the planned web local store for M001 behind repository boundaries. | This keeps the web proof surface closer to the long-term offline model while remaining browser-testable in the first milestone. | Yes — if implementation evidence shows another browser-local persistence layer is materially more reliable or simpler without changing domain seams. | human |
| D006 | M001/S01 planning | architecture | Schema authority for M001/S01 auth and access work | Use `supabase/migrations` and `supabase/seed.sql` as the authoritative schema and RLS source for S01; keep `packages/db` compile-safe but non-authoritative until a later slice or milestone explicitly assigns shared DB package ownership. | The repo has no working shared DB layer today, `packages/db` currently contains a broken tenant helper, and secure access in this slice depends on real Supabase auth/RLS behavior. A single SQL source of truth avoids split ownership while still leaving `packages/db` available for narrow helpers and types. | Yes | agent |
| D007 | M001/S01/T03 | architecture | How protected calendar routes derive permitted scope in the web app shell | Load memberships and permitted calendars once in the protected `(app)` layout, then have `/calendars/[calendarId]` resolve access only from that trusted parent scope and render an explicit denied surface when the id is outside it. | This avoids duplicated nested queries at the slice's 10x breakpoint, keeps route authorization tied to already-trusted server data instead of ad hoc per-page lookups, and guarantees guessed or stale calendar ids fail closed with a visible reason/phase instead of silent empty data. | Yes | agent |
| D008 | M001/S01 | security | Trusted server-side auth resolution for SvelteKit requests | Treat cookie-backed Supabase sessions as untrusted until `getUser()` revalidates them; expose only minimal trusted auth state from `event.locals` and root layout data. | Supabase SSR sessions originate from cookies and can be stale or forged if treated as server truth. Revalidating with `getUser()` preserves a fail-closed auth boundary for protected routes and keeps downstream slices anchored to authenticated server data instead of client-controlled session payloads. | Yes | agent |
| D009 | M001/S02 planning | architecture | Scheduling persistence and edit authority for browser calendar flows | Use concrete SQL-backed `shifts` rows as the primary editable occurrences, optionally linked to bounded `shift_series` metadata, and keep create/edit/move/delete server-mediated on the trusted `/calendars/[calendarId]` route. | Concrete occurrences preserve the S01 trust boundary, allow multiple same-day shifts and deterministic move/delete behavior, and give later offline, sync, and conflict slices authoritative rows to reconcile instead of synthetic events. | Yes | agent |
| D010 | M001/S02 planning | library | Recurrence and calendar UI implementation approach for S02 | Use the `rrule` library for bounded recurrence parsing/expansion and ship a custom Svelte week board with explicit move/edit controls as the default browser UX; treat drag-and-drop/full calendar libraries as a later fallback, not the first-cut dependency. | `rrule` avoids fragile custom recurrence math, while a custom board fits Caluno's existing visual language and keeps the first browser editing surface clearer and lower-risk than integrating a heavily themed stock calendar widget. | Yes | agent |
| D011 | M001/S02/T01 | architecture | Schedule persistence model for recurring shifts | Persist recurring schedules as concrete rows in public.shifts, with optional bounded provenance metadata in public.shift_series and composite series/calendar linkage | Later browser editing, conflict inspection, and route-scoped authorization all need real timestamped rows that can render multiple same-day and overlapping shifts without virtual expansion at read time. The separate series record preserves bounded recurrence intent for future create/edit flows while keeping access control anchored to the existing calendar-membership boundary. | Yes | agent |
| D012 | M001/S02/T02 | architecture | How the calendar schedule route scopes data loads and authorizes schedule mutations | Load schedule data one validated visible week at a time on `/calendars/[calendarId]`, and require create/edit/move/delete actions to derive calendar and shift authority from the trusted route scope on the server instead of accepting client-authoritative calendar or group ids. | This preserves the S01 access boundary, prevents cross-calendar writes from route-local form submissions, keeps the 7-day query cost bounded, and surfaces named loader/action failure reasons for later browser proof and debugging. | Yes | agent |
| D013 | M001/S02/T03 | architecture | How schedule create actions should return deterministic ids under Supabase RLS | Generate shift/series ids in the trusted server action and insert without `.select()` for create paths | Local Supabase RLS allowed authenticated inserts but blocked `insert().select()` row representation for newly created schedule rows. Generating ids server-side lets create actions return deterministic affected ids and success states without weakening RLS or requiring service-role access. | Yes | agent |
| D014 | M001/S02/T04 | observability | How browser schedule proof should retain diagnostics for shared-calendar editing flows | Capture phase, calendar/week context, focused shift ids, console/page errors, and failed responses centrally in the shared Playwright fixture and attach them as flow diagnostics for every schedule E2E run. | Centralized diagnostics make browser-proof failures reproducible without ad-hoc logging, preserve the slice's protected-route context, and keep retained artifacts focused on seeded ids and phase metadata instead of secrets. | Yes | agent |
| D015 | M001/S03 planning | architecture | Offline continuity authority boundary for cached sessions and local schedule persistence | Keep trusted SSR auth and server-authoritative online schedule access unchanged, but add a client-only offline continuity path that unlocks only previously synced permitted calendars from a browser-local repository plus cached app-shell snapshot. | This satisfies R001 and R004 without weakening the S01/S02 fail-closed auth and membership boundary. Cached session/snapshot data becomes continuity evidence for this browser, not new authority for guessed ids, changed memberships, or unsynced calendars, and the repository seam prepares S04 sync work. | Yes | agent |
| D016 | M001/S03 planning | verification | Proof surface for offline browser continuity | Use a build/preview-backed Playwright config, separate from the default Vite dev-server config, for service-worker and offline-reopen verification. | The existing dev-server proof is sufficient for online route behavior but is not a trustworthy surface for service-worker caching, cross-origin isolation, and offline reopen after restart. A preview-backed proof more closely matches the real runtime needed by S03. | Yes | agent |
| D017 | M001/S03/T01 | architecture | How offline continuity proof should be exercised in S03 | Use a dedicated Playwright offline config that builds the app, starts vite preview on its own port with reuseExistingServer disabled, and exposes service-worker/isolation status through a root runtime surface. | Service-worker caching and cross-origin isolation are part of the feature contract, so proving offline reopen against a dev server or a reused process would hide exactly the failures this slice is meant to catch and could blur the S01/S02 authorization boundary. | Yes | agent |
| D018 | M001/S03/T02 | architecture | Offline continuity cache and repository boundary for S03 | Store only a minimal cached app-shell snapshot (viewer, sanitized session expiry, groups, permitted calendars, primary calendar id, onboarding state) in browser storage, and keep week snapshots plus local mutations behind the offline repository seam. | This preserves the S01/S02 trust boundary because offline continuity reopens only previously trusted scope on this browser, avoids persisting raw Supabase access/refresh tokens, and gives later offline/local-first tasks a single repository surface for durable week state and queued edits. | Yes | agent |
| D019 | M001/S03/T03 | routing | How protected routes determine online versus offline continuity state | Persist app-shell and week snapshots only from online browser loads, and resolve offline protected routes from browser-local cached scope/repository instead of trusting serialized SSR payloads. | Service-worker-cached HTML can embed old server data, so the route must reclassify itself using current online status plus browser-local continuity data to expose trusted-online, cached-offline, and fail-closed offline-denied states accurately. | Yes | agent |
| D020 | M001/S03/T04 | architecture | How offline local-first calendar continuity should preserve visible week state across reloads | Persist the current visible-week snapshot after each local mutation and keep the mutation queue focused on pending/retryable sync state rather than requiring queue replay to reconstruct the board | This keeps offline reload continuity aligned with the existing cached-route loader, lets the board reopen with local edits already materialized, and still preserves explicit sync diagnostics and retry state for server confirmation without widening route authority. | Yes | agent |
| D021 | M001/S03 wrap-up | library | How the web offline repository should bootstrap sqlite-wasm under Vite | Use @sqlite.org/sqlite-wasm through the package's wrapped worker promiser and exclude the package from Vite optimizeDeps, instead of bootstrapping a project-owned worker through guessed named exports. | The package's browser/Vite path exposes worker bootstrap behavior differently from Node and from the raw ESM files. Using the library-supported wrapped worker path plus optimizeDeps exclusion avoids mismatched export shapes and missing sqlite3.wasm asset resolution during browser-local repository startup. | Yes | agent |
| D022 | M001/S04 planning | architecture | S04 sync and live-update composition | Implement S04 as a controller-centered browser sync layer that (1) rebases pending queue entries onto refreshed trusted week snapshots before any invalidation can replace local state, (2) drains reconnect work sequentially through the existing `/calendars/[calendarId]` route actions, and (3) uses Supabase Realtime only as change detection that triggers trusted refresh plus local replay. | The queue already persists enough mutation detail to deterministically rebuild the local-first board, while S02 decisions D012/D013 keep mutation authority on trusted SvelteKit route actions. Rebase-first refresh prevents the current `server-sync` caching path from erasing pending local work, and treating Realtime as change detection avoids inventing a second client-authoritative write path. | Yes | agent |
| D023 | M001/S04/T01 | architecture | Shared calendar refresh/replay policy location | Centralize trusted refresh replay and snapshot-write guard logic in apps/web/src/lib/offline/sync-engine.ts and have both the controller and route loader call it. | The same deterministic queue semantics must protect both browser-local snapshot persistence and later trusted refresh ingestion. A single pure module makes failure reasons testable, keeps local-write protection consistent across paths, and gives downstream reconnect/realtime work one contract to reuse. | Yes | agent |
| D024 | M001/S04/T02 | architecture | Reconnect queue flush transport for shared calendar offline sync | Drain queued reconnect mutations by rebuilding form posts to the existing `/calendars/[calendarId]` named route actions and normalize responses with SvelteKit `deserialize` into the existing `ScheduleActionState` contract. | This preserves the S02 server-authority boundary, keeps offline replay on the same trusted write path as enhanced forms, avoids a second browser-only mutation API, and lets reconnect failures surface through the same typed success/forbidden/timeout/malformed semantics already used by the route. | Yes | agent |
| D025 | M001/S04/T03 | architecture | How browser realtime signals refresh shared calendar weeks in S04/T03 | Handle shared shift realtime events as browser-side change detection that invalidates the current trusted calendar route and then ingests the refreshed schedule through the existing replay helper, instead of applying row payloads directly to the board. | Supabase Realtime payloads are not authoritative enough to mutate the board safely under local pending writes, delete payloads can be incomplete under RLS unless treated conservatively, and the controller already owns deterministic trusted-refresh replay. Reusing route invalidation plus controller ingestion preserves the S02 trust boundary while keeping pending local work visible. | Yes | agent |
